# ============================================
# Temetra CSV Validation Tool - Environment Variables
# Copyright (c) 2024 Vanzora, LLC. All rights reserved.
# ============================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual credentials
# 3. NEVER commit .env.local to version control (it's in .gitignore)
#
# ============================================

# --------------------------------------------
# SUPABASE CONFIGURATION
# Get these from: https://supabase.com/dashboard
# Go to: Your Project → Settings → API
# --------------------------------------------

# The URL of your Supabase project
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# The "anon" public key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# The "service_role" secret key (NEVER expose this in browser - server only!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# --------------------------------------------
# EMAIL CONFIGURATION (Optional)
# For sending validation result emails
# --------------------------------------------

# SMTP server settings (example: Gmail, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email sender info
EMAIL_FROM_NAME=Temetra CSV Validator
EMAIL_FROM_ADDRESS=noreply@yourdomain.com

# Admin email (receives upload notifications)
ADMIN_EMAIL=admin@yourdomain.com

# --------------------------------------------
# APPLICATION SETTINGS
# --------------------------------------------

# Base URL of the application (for email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Enable/disable demo mode (shows demo credentials on login)
NEXT_PUBLIC_DEMO_MODE=true
